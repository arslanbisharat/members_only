<% @posts.each do | post |%>
  <div class="container">	  <div class="section">
    <div class="columns is-centered">	    <div class="container">
      <div class="column is-4">	      <div class="columns is-centered">
        <p>	        <div class="column is-4">
          <strong>Author:</strong>	          <% if user_signed_in? %>
          <%= @post.user.name %>	            <p>
        </p>	              <strong>Author:</strong>
        <p>	              <%= post.user.name %>
          <strong>Title:</strong>	            </p>
          <%= @post.title %>	          <% end %>
        </p>	          <p>
        <p>	            <strong>Title:</strong>
          <strong>Body:</strong>	            <%= post.title %>
          <%= @post.body %>	          </p>
        </p>	          <p>
        <%= link_to 'Edit', edit_post_path(@post) %> |	            <strong>Body:</strong>
            <%= post.body %>
          </p>
          <% if user_signed_in? %>
            <%= link_to 'Edit', edit_post_path(post) %> | 
            <%= link_to 'Delete', post, method: :delete, data: { confirm: "Are you sure you want to delete this tweeet?" } %>
          <% end %>
        </div>
      </div>
    </div>
  </div>
<% end %>
